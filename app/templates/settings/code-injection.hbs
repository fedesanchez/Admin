<section class="gh-canvas">
    <GhCanvasHeader class="gh-canvas-header">
        <h2 class="gh-canvas-title" data-test-screen-title>
            Código extra
        </h2>
        <section class="view-actions">
            <GhTaskButton @task={{this.saveTask}} @class="gh-btn gh-btn-blue gh-btn-icon" data-test-save-button={{true}} />
        </section>
    </GhCanvasHeader>

    {{#if this.showLeaveSettingsModal}}
        <GhFullscreenModal @modal="leave-settings"
            @confirm={{action "leaveSettings"}}
            @close={{action "toggleLeaveSettingsModal"}}
            @modifier="action wide" />
    {{/if}}

    <section class="view-container">
        <form id="settings-code" novalidate="novalidate">
            <fieldset>
                <p class="gh-box gh-box-tip">
                    {{svg-jar "idea"}}
                    Ghost le permite inyectar código en la parte superior e inferior de los archivos de su tema sin editarlos. Esto permite modificaciones rápidas para insertar elementos útiles como códigos de seguimiento y metaetiquetas.
                </p>

                <div class="br3 shadow-1 bg-grouped-table pa5 pt3 pb1">
                    <div class="form-group settings-code">
                        <label for="ghost-head">Header del sitio</label>
                        <p>El código aquí se inyectará en la etiqueta <code>\{{ghost_head}}</code> en cada página del sitio</p>
                        <GhCmEditor @value={{this.settings.codeinjectionHead}} @id="ghost-head" @class="gh-input settings-code-editor" @name="codeInjection[ghost_head]" @type="text" @update={{action (mut this.settings.codeinjectionHead)}} />
                    </div>

                    <div class="form-group settings-code">
                        <label for="ghost-foot">Footer del sitio</label>
                        <p>El código aquí se inyectará en la etiqueta <code>\{{ghost_foot}}</code> en cada página del sitio</p>
                        <GhCmEditor @value={{this.settings.codeinjectionFoot}} @id="ghost-foot" @class="gh-input settings-code-editor" @name="codeInjection[ghost_foot]" @type="text" @update={{action (mut this.settings.codeinjectionFoot)}} />
                    </div>
                </div>
            </fieldset>
        </form>
    </section>
</section>
